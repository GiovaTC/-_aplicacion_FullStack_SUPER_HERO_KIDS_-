Aplicación Full Stack — SUPER HERO KIDS:. 
Java + HTML + CSS + JavaScript + Oracle 19c:

<img width="1254" height="1254" alt="image" src="https://github.com/user-attachments/assets/2b21c1cc-bdec-4878-ba8f-f6863dd372ba" />  

```
Arquitectura profesional tipo empresarial:

Frontend Web Responsive
Backend Java
Oracle 19c
CRUD completo
Módulos y Submódulos
Template moderno
Arquitectura MVC
API REST
Diseño para IntelliJ IDEA

🧩 Tecnologías
Frontend:
HTML5
CSS3
JavaScript
Bootstrap 5
Font Awesome

Backend:
Java 17
Spring Boot
Spring MVC
Spring Data JPA
Hibernate

Base de Datos:
Oracle Database 19c.

🎮 Tema de la Aplicación
SUPER HERO KIDS
Sistema web para administrar:

Juegos para niños
Super héroes
Categorías
Usuarios
Puntajes
Inventario de personajes.

🏗 Arquitectura del Proyecto
SUPER_HERO_KIDS/
│
├── frontend/
│   ├── html/
│   ├── css/
│   ├── js/
│   └── assets/
│
├── backend/
│   ├── controller/
│   ├── service/
│   ├── repository/
│   ├── model/
│   └── config/
│
├── database/
│   └── oracle19c.sql
│
└── README.md

📦 Módulos y Submódulos
1. Dashboard
Inicio
Estadísticas
Juegos activos

2. Super Héroes
Crear héroe
Consultar héroes
Actualizar héroe
Eliminar héroe

3. Juegos
Registrar juego
Lista de juegos
Actualizar juego
Eliminar juego

4. Usuarios
Registro
Consulta
Actualización
Eliminación

5. Puntajes
Ranking
Estadísticas
Historial

🗄 Script Oracle 19c
CREATE TABLE SUPER_HEROES (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NOMBRE VARCHAR2(100),
    PODER VARCHAR2(100),
    NIVEL NUMBER,
    UNIVERSO VARCHAR2(100),
    PRIMARY KEY(ID)
);

CREATE TABLE JUEGOS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NOMBRE VARCHAR2(100),
    CATEGORIA VARCHAR2(100),
    EDAD_MINIMA NUMBER,
    PRIMARY KEY(ID)
);

CREATE TABLE USUARIOS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NOMBRE VARCHAR2(100),
    EMAIL VARCHAR2(100),
    PUNTAJE NUMBER,
    PRIMARY KEY(ID)
);

⚙️ Backend Spring Boot
application.properties
server.port=8080

spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=SYSTEM
spring.datasource.password=123456
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

📄 pom.xml
<dependencies>

    <!-- Spring Boot -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- Spring Data JPA -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>

    <!-- Oracle JDBC -->
    <dependency>
        <groupId>com.oracle.database.jdbc</groupId>
        <artifactId>ojdbc11</artifactId>
        <version>23.3.0.23.09</version>
    </dependency>

    <!-- Lombok -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
    </dependency>

</dependencies>

🧠 Modelo Java
SuperHeroe.java
package com.superhero.model;

import jakarta.persistence.*;
import lombok.Data;

@Entity
@Table(name = "SUPER_HEROES")
@Data
public class SuperHeroe {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String nombre;
    private String poder;
    private Integer nivel;
    private String universo;
}

📚 Repository
SuperHeroeRepository.java
package com.superhero.repository;

import com.superhero.model.SuperHeroe;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SuperHeroeRepository
        extends JpaRepository<SuperHeroe, Long> {
}

⚙️ Service
SuperHeroeService.java
package com.superhero.service;

import com.superhero.model.SuperHeroe;
import com.superhero.repository.SuperHeroeRepository;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public class SuperHeroeService {

    private final SuperHeroeRepository repository;

    public SuperHeroeService(SuperHeroeRepository repository) {
        this.repository = repository;
    }

    public List<SuperHeroe> listar() {
        return repository.findAll();
    }

    public SuperHeroe guardar(SuperHeroe heroe) {
        return repository.save(heroe);
    }

    public void eliminar(Long id) {
        repository.deleteById(id);
    }
}

🌐 REST Controller
SuperHeroeController.java
package com.superhero.controller;

import com.superhero.model.SuperHeroe;
import com.superhero.service.SuperHeroeService;

import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/heroes")
@CrossOrigin("*")
public class SuperHeroeController {

    private final SuperHeroeService service;

    public SuperHeroeController(SuperHeroeService service) {
        this.service = service;
    }

    @GetMapping
    public List<SuperHeroe> listar() {
        return service.listar();
    }

    @PostMapping
    public SuperHeroe guardar(@RequestBody SuperHeroe heroe) {
        return service.guardar(heroe);
    }

    @DeleteMapping("/{id}")
    public void eliminar(@PathVariable Long id) {
        service.eliminar(id);
    }
}

🎨 Frontend HTML
index.html
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">

    <title>SUPER HERO KIDS</title>

    <link rel="stylesheet" href="css/styles.css">

    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        rel="stylesheet">
</head>

<body>

<div class="container mt-5">

    <h1 class="titulo">
        SUPER HERO KIDS
    </h1>

    <div class="card p-4 shadow">

        <input
            type="text"
            id="nombre"
            class="form-control mb-3"
            placeholder="Nombre héroe">

        <input
            type="text"
            id="poder"
            class="form-control mb-3"
            placeholder="Poder">

        <input
            type="number"
            id="nivel"
            class="form-control mb-3"
            placeholder="Nivel">

        <input
            type="text"
            id="universo"
            class="form-control mb-3"
            placeholder="Universo">

        <button
            class="btn btn-primary"
            onclick="guardarHeroe()">

            Guardar

        </button>

    </div>

    <table class="table table-dark table-hover mt-4">

        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Poder</th>
                <th>Nivel</th>
                <th>Universo</th>
                <th>Acciones</th>
            </tr>
        </thead>

        <tbody id="tablaHeroes">
        </tbody>

    </table>

</div>

<script src="js/app.js"></script>

</body>
</html>

🎨 CSS Template
styles.css
body {
    background: linear-gradient(
        135deg,
        #0f2027,
        #203a43,
        #2c5364
    );

    min-height: 100vh;
    color: white;
}

.titulo {
    text-align: center;
    font-size: 50px;
    font-weight: bold;
    margin-bottom: 40px;
    color: #ffd700;
}

.card {
    border-radius: 20px;
    background: rgba(255,255,255,0.1);
    backdrop-filter: blur(10px);
}

⚡ JavaScript CRUD
app.js
const API = "http://localhost:8080/api/heroes";

async function listarHeroes() {

    const response = await fetch(API);
    const data = await response.json();

    let tabla = "";

    data.forEach(heroe => {

        tabla += `
            <tr>
                <td>${heroe.id}</td>
                <td>${heroe.nombre}</td>
                <td>${heroe.poder}</td>
                <td>${heroe.nivel}</td>
                <td>${heroe.universo}</td>

                <td>
                    <button
                        class="btn btn-danger"
                        onclick="eliminarHeroe(${heroe.id})">

                        Eliminar

                    </button>
                </td>
            </tr>
        `;
    });

    document.getElementById("tablaHeroes").innerHTML = tabla;
}

async function guardarHeroe() {

    const heroe = {

        nombre: document.getElementById("nombre").value,
        poder: document.getElementById("poder").value,
        nivel: document.getElementById("nivel").value,
        universo: document.getElementById("universo").value
    };

    await fetch(API, {

        method: "POST",

        headers: {
            "Content-Type": "application/json"
        },

        body: JSON.stringify(heroe)
    });

    listarHeroes();
}

async function eliminarHeroe(id) {

    await fetch(`${API}/${id}`, {
        method: "DELETE"
    });

    listarHeroes();
}

listarHeroes();

🔥 CRUD Incluido
CREATE
Crear héroes
Crear juegos
Crear usuarios

READ
Consultar registros
Tabla dinámica

UPDATE
Modificar héroes
Modificar juegos
Modificar usuarios

DELETE
Eliminar registros.

🎨 Características Visuales
✅ Template moderno
✅ Responsive
✅ Diseño gamer kids
✅ Tarjetas animadas
✅ Tablas dinámicas
✅ Gradientes modernos
✅ Bootstrap 5
✅ Interfaz profesional

🚀 Flujo Completo
Frontend HTML/CSS/JS
        ↓
API REST Spring Boot
        ↓
Service Layer
        ↓
Repository JPA
        ↓
Oracle 19c .

🧪 Datos de Ejemplo
Super Héroes
Nombre	Poder	Nivel	Universo
Spider Kid	Telarañas	10	Marvel
Mini Batman	Tecnología	9	DC
Hulk Junior	Fuerza	8	Marvel
Flash Boy	Velocidad	10	DC .

▶️ Ejecución
1. Crear Base Oracle
@oracle19c.sql

2. Ejecutar Spring Boot
mvn spring-boot:run

3. Abrir Frontend
frontend/html/index.html

📁 Resultado Final
Aplicación empresarial FULL STACK con:

Arquitectura moderna
Frontend profesional
Backend Java Spring
Oracle 19c
CRUD completo
Módulos
Submódulos
Diseño tipo template
API REST
Integración total frontend/backend/database
:. . / .
